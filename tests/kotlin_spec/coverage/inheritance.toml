[[requirements]]
id = "KS-INHERITANCE-0001"
statement = "The inherited-from classifier is the base type and the inheriting classifier is the derived type; a class or object may have one class direct superclass and multiple interface base types."
classification = "exact"
capabilities = ["implementation", "definition", "document symbols"]
status = "active"
tests = ["ks_inheritance_0001_class_has_one_superclass_and_multiple_interface_base_types"]
duplicates = ["ks_declarations_0010_supertype_specifiers_create_indexed_inheritance_edges"]
fixture = "DerivedSpec inherits BaseSpec plus FirstSpec and SecondSpec interfaces, with UnrelatedSpec as a decoy."

[[requirements]]
id = "KS-INHERITANCE-0002"
statement = "A class or object cannot inherit more than one class type."
classification = "exact"
capabilities = ["syntax diagnostics", "implementation"]
status = "ignored"
tests = ["ks_inheritance_0002_class_cannot_inherit_multiple_class_types"]
duplicates = []
fixture = "ValidSpec combines class and interface; InvalidSpec invokes two open class supertypes."
ignore_reason = "Observed red: two class supertypes produce a clean CST and no semantic diagnostic."
observed_failure = "two class supertypes produce a clean CST and no semantic diagnostic."
expected_behavior = "SecondBaseSpec must receive a multiple-superclass diagnostic."

[[requirements]]
id = "KS-INHERITANCE-0004"
statement = "A class not explicitly open or abstract is closed and cannot be inherited."
classification = "exact"
capabilities = ["syntax diagnostics", "implementation"]
status = "ignored"
tests = ["ks_inheritance_0004_closed_class_cannot_be_inherited"]
duplicates = []
fixture = "OpenSpec and AbstractSpec accept subtypes; default ClosedSpec competes with InvalidSpec."
ignore_reason = "Observed red: inheritance from default-closed ClosedSpec has a clean CST and no semantic diagnostic."
observed_failure = "inheritance from default-closed ClosedSpec has a clean CST and no semantic diagnostic."
expected_behavior = "ClosedSpec in InvalidSpec's supertype list must receive a diagnostic."

[[requirements]]
id = "KS-INHERITANCE-0005"
statement = "Data, enum, and annotation classes cannot be declared open or abstract."
classification = "exact"
capabilities = ["syntax diagnostics", "document symbols"]
status = "ignored"
tests = ["ks_inheritance_0005_data_enum_and_annotation_classes_are_always_closed"]
duplicates = []
fixture = "Plain declarations compete with open and abstract modifiers for each of the three class kinds."
ignore_reason = "Observed red: open data class has a clean CST and no semantic diagnostic."
observed_failure = "open data class has a clean CST and no semantic diagnostic."
expected_behavior = "Every open or abstract modifier on data, enum, and annotation classes must receive a diagnostic."

[[requirements]]
id = "KS-INHERITANCE-0006"
statement = "Data, enum, and annotation class types are always closed and cannot be inherited from."
classification = "exact"
capabilities = ["syntax diagnostics", "implementation"]
status = "ignored"
tests = ["ks_inheritance_0006_data_enum_and_annotation_types_cannot_be_inherited"]
duplicates = ["ks_inheritance_0005_data_enum_and_annotation_classes_are_always_closed"]
fixture = "Each special class kind competes with a direct subtype declaration."
ignore_reason = "Observed red: inheritance from DataSpec has a clean CST and no semantic diagnostic."
observed_failure = "inheritance from DataSpec has a clean CST and no semantic diagnostic."
expected_behavior = "Every subtype of DataSpec, EnumSpec, or AnnotationSpec must receive a diagnostic."

[[requirements]]
id = "KS-INHERITANCE-0007"
statement = "An interface may inherit any number of interface types and no class types, provided the result is well-formed."
classification = "exact"
capabilities = ["syntax diagnostics", "implementation"]
status = "ignored"
tests = ["ks_inheritance_0007_interface_inherits_any_number_of_interfaces_only"]
duplicates = []
fixture = "DerivedSpec inherits two interfaces; InvalidSpec names open class BaseSpec as its base."
ignore_reason = "Observed red: interface inheritance from BaseSpec has a clean CST and no semantic diagnostic."
observed_failure = "interface inheritance from BaseSpec has a clean CST and no semantic diagnostic."
expected_behavior = "BaseSpec in InvalidSpec's base list must receive a diagnostic."

[[requirements]]
id = "KS-INHERITANCE-0008"
statement = "Object types cannot be inherited from."
classification = "exact"
capabilities = ["syntax diagnostics", "implementation"]
status = "ignored"
tests = ["ks_inheritance_0008_object_type_cannot_be_inherited"]
duplicates = []
fixture = "RegistrySpec object competes with InvalidSpec invoking it as a superclass."
ignore_reason = "Observed red: inheritance from RegistrySpec has a clean CST and no semantic diagnostic."
observed_failure = "inheritance from RegistrySpec has a clean CST and no semantic diagnostic."
expected_behavior = "RegistrySpec in InvalidSpec's supertype list must receive a diagnostic."

[[requirements]]
id = "KS-INHERITANCE-0010"
source_anchor = "#abstract-classes-inheritance"
statement = "An abstract class cannot be instantiated directly."
classification = "exact"
capabilities = ["syntax diagnostics", "completion", "signature help"]
status = "ignored"
tests = ["ks_inheritance_0010_abstract_class_cannot_be_instantiated_directly"]
duplicates = []
fixture = "DerivedSpec construction competes with direct BaseSpec construction."
ignore_reason = "Observed red: direct BaseSpec construction has a clean CST and no semantic diagnostic."
observed_failure = "direct BaseSpec construction has a clean CST and no semantic diagnostic."
expected_behavior = "BaseSpec() must receive an abstract-instantiation diagnostic."

[[requirements]]
id = "KS-INHERITANCE-0011"
statement = "An abstract class is implicitly open and intended for inheritance."
classification = "exact"
capabilities = ["implementation", "document symbols"]
status = "active"
tests = ["ks_inheritance_0011_abstract_class_is_implicitly_open"]
duplicates = []
fixture = "DerivedSpec extends abstract BaseSpec and produces one exact subtype edge."

[[requirements]]
id = "KS-INHERITANCE-0012"
statement = "An abstract class may declare abstract properties and functions."
classification = "exact"
capabilities = ["syntax diagnostics", "document symbols", "implementation"]
status = "active"
tests = ["ks_inheritance_0012_abstract_class_accepts_abstract_properties_and_functions"]
duplicates = []
fixture = "BaseSpec declares abstract valueSpec and renderSpec without implementations."

[[requirements]]
id = "KS-INHERITANCE-0013"
statement = "A class or non-functional interface may be declared sealed."
classification = "exact"
capabilities = ["syntax diagnostics", "document symbols", "implementation"]
status = "active"
tests = ["ks_inheritance_0013_class_and_interface_may_be_sealed"]
duplicates = ["ks_inheritance_0015_sealed_class_is_implicitly_abstract_and_modifiers_are_exclusive"]
fixture = "SealedClassSpec and SealedInterfaceSpec each have a concrete top-level leaf."
[[requirements.documentation_citations]]
repository = "JetBrains/kotlin-web-site"
revision = "7c270c2ac320fbee4884927f056b89d32f2a002e"
source_path = "docs/topics/inheritance.md"

[[requirements.documentation_citations]]
repository = "JetBrains/kotlin-web-site"
revision = "7c270c2ac320fbee4884927f056b89d32f2a002e"
source_path = "docs/topics/sealed-classes.md"

[[requirements]]
id = "KS-INHERITANCE-0014"
statement = "A functional interface cannot be declared sealed."
classification = "exact"
capabilities = ["syntax diagnostics", "document symbols"]
status = "ignored"
tests = ["ks_inheritance_0014_functional_interface_cannot_be_sealed"]
duplicates = ["ks_4_1_2_001_functional_interface_has_single_abstract_function_contract"]
fixture = "Baseline fun interface ValidSpec must parse before sealed fun interface InvalidSpec is rejected."
ignore_reason = "Observed red in the harness prerequisite: tree-sitter-kotlin rejects the baseline fun interface form, so the sealed restriction cannot yet be isolated."
observed_failure = "Observed red in the harness prerequisite: tree-sitter-kotlin rejects the baseline fun interface form, so the sealed restriction cannot yet be isolated."
expected_behavior = "ValidSpec must parse cleanly and only sealed functional InvalidSpec must receive a diagnostic."

[[requirements]]
id = "KS-INHERITANCE-0015"
statement = "sealed implicitly makes a class abstract, and sealed and abstract modifiers are mutually exclusive."
classification = "exact"
capabilities = ["syntax diagnostics", "implementation"]
status = "ignored"
tests = ["ks_inheritance_0015_sealed_class_is_implicitly_abstract_and_modifiers_are_exclusive"]
duplicates = []
fixture = "DerivedSpec extends sealed SealedSpec; InvalidSpec explicitly combines sealed abstract."
ignore_reason = "Observed red: sealed abstract InvalidSpec has a clean CST and no semantic diagnostic."
observed_failure = "sealed abstract InvalidSpec has a clean CST and no semantic diagnostic."
expected_behavior = "The redundant abstract modifier must receive a diagnostic."
[[requirements.documentation_citations]]
repository = "JetBrains/kotlin-web-site"
revision = "7c270c2ac320fbee4884927f056b89d32f2a002e"
source_path = "docs/topics/sealed-classes.md"

[[requirements]]
id = "KS-INHERITANCE-0016"
statement = "A sealed type may be inherited by a fully-qualified type in the same package and module."
classification = "exact"
capabilities = ["implementation", "definition", "workspace symbols"]
status = "active"
tests = ["ks_inheritance_0016_sealed_type_accepts_same_package_and_module_subtype"]
duplicates = []
fixture = "Top-level LeafSpec and SealedSpec are in package states under distinct paths of module-a."

[[requirements]]
id = "KS-INHERITANCE-0017"
statement = "A sealed type cannot be inherited from a different package."
classification = "exact"
capabilities = ["syntax diagnostics", "implementation"]
status = "ignored"
tests = ["ks_inheritance_0017_sealed_type_rejects_different_package_subtype"]
duplicates = []
fixture = "module-a SealedSpec is in package first while imported LeafSpec is in package second."
ignore_reason = "Observed red: kmp-lsp indexes LeafSpec as a subtype despite the distinct package."
observed_failure = "kmp-lsp indexes LeafSpec as a subtype despite the distinct package."
expected_behavior = "LeafSpec must receive a sealed-package diagnostic and must not appear as an implementation."

[[requirements]]
id = "KS-INHERITANCE-0018"
statement = "A sealed type cannot be inherited from a different module."
classification = "exact"
capabilities = ["syntax diagnostics", "implementation"]
status = "ignored"
tests = ["ks_inheritance_0018_sealed_type_rejects_different_module_subtype"]
duplicates = []
fixture = "Same-package SealedSpec and LeafSpec reside under module-a and module-b URI roots."
ignore_reason = "Observed red: kmp-lsp has no module ownership model and indexes the module-b subtype."
observed_failure = "kmp-lsp has no module ownership model and indexes the module-b subtype."
expected_behavior = "LeafSpec must receive a sealed-module diagnostic and must not appear as an implementation."

[[requirements]]
id = "KS-INHERITANCE-0019"
statement = "A sealed subtype must have a fully-qualified name; local and anonymous types cannot inherit sealed types."
classification = "exact"
capabilities = ["syntax diagnostics", "implementation", "document symbols"]
status = "ignored"
tests = ["ks_inheritance_0019_sealed_type_rejects_local_and_anonymous_subtypes"]
duplicates = []
fixture = "TopLevelSpec competes with LocalSpec and an anonymous object inheriting SealedSpec."
ignore_reason = "Observed red: local LocalSpec inheritance has a clean CST and no semantic diagnostic."
observed_failure = "local LocalSpec inheritance has a clean CST and no semantic diagnostic."
expected_behavior = "LocalSpec and the anonymous object must receive sealed-subtype diagnostics."

[[requirements]]
id = "KS-INHERITANCE-0023"
statement = "Most built-in class types are closed and cannot be inherited from."
classification = "exact"
capabilities = ["syntax diagnostics", "implementation"]
status = "ignored"
tests = ["ks_inheritance_0023_closed_builtin_class_types_cannot_be_inherited"]
duplicates = []
fixture = "Neutral ValidSpec competes with direct String, Int, and Boolean subclass declarations."
ignore_reason = "Observed red: StringSpec inheritance has a clean CST and no semantic diagnostic."
observed_failure = "StringSpec inheritance has a clean CST and no semantic diagnostic."
expected_behavior = "Every direct subtype of String, Int, or Boolean must receive a closed-type diagnostic."

[[requirements]]
id = "KS-INHERITANCE-0024"
statement = "Function types are treated as interfaces and may be inherited as such."
classification = "exact"
capabilities = ["syntax diagnostics", "document symbols", "implementation"]
status = "active"
tests = ["ks_inheritance_0024_function_type_is_inheritable_as_interface"]
duplicates = []
fixture = "HandlerSpec inherits (Int) -> String and implements invoke with an exact signature."

[[requirements]]
id = "KS-INHERITANCE-0025"
statement = "Matching callable declarations must have the same name."
classification = "heuristic"
capabilities = ["implementation", "definition"]
status = "active"
tests = ["ks_inheritance_0025_matching_callable_requires_same_name"]
duplicates = []
fixture = "BaseSpec and DerivedSpec each contain renderSpec and competingSpec; implementation query selects only renderSpec."
heuristic_limitations = "Covers direct indexed Kotlin function overrides with explicit override modifier; properties, Java, aliases, indirect hierarchies, and generated members are excluded."

[[requirements]]
id = "KS-INHERITANCE-0026"
statement = "Matching callables must both be properties or both be functions."
classification = "heuristic"
capabilities = ["implementation", "document symbols"]
status = "ignored"
tests = ["ks_inheritance_0026_matching_callable_requires_same_declaration_kind"]
duplicates = []
fixture = "Base property stateSpec competes with derived override property and same-named override function."
heuristic_limitations = "Covers a direct Kotlin base property and derived property/function candidates; accessors, Java fields, synthetic properties, generics, and indirect inheritance are excluded."
ignore_reason = "Observed red: implementation lookup returns no property implementation because kmp-lsp only collects override functions."
observed_failure = "implementation lookup returns no property implementation because kmp-lsp only collects override functions."
expected_behavior = "The derived property must be returned and the same-named function must be excluded."

[[requirements]]
id = "KS-INHERITANCE-0027"
statement = "Matching function declarations must have matching function signatures."
classification = "heuristic"
capabilities = ["implementation", "signature help", "hover"]
status = "ignored"
tests = ["ks_inheritance_0027_matching_functions_require_matching_signatures"]
duplicates = []
fixture = "Base and derived types each overload selectSpec with explicit Int and String parameter types."
heuristic_limitations = "Covers one direct override with equal arity and explicit simple parameter types; generics, receivers, suspend, defaults, aliases, varargs, substitution, and return compatibility are excluded."
ignore_reason = "Observed red: implementation lookup returns both Int and String overloads for the Int base declaration."
observed_failure = "implementation lookup returns both Int and String overloads for the Int base declaration."
expected_behavior = "Only the derived Int selectSpec overload must be returned."

[[requirements]]
id = "KS-INHERITANCE-0029"
statement = "A matching declaration in a derived classifier subsumes the base declaration when the base owner is its supertype."
classification = "heuristic"
capabilities = ["implementation", "definition"]
status = "active"
tests = ["ks_inheritance_0029_derived_matching_declaration_subsumes_base_declaration"]
duplicates = []
fixture = "DerivedSpec directly extends BaseSpec and overrides the exact explicit renderSpec(Int) signature."
heuristic_limitations = "Covers one direct Kotlin class edge and explicit same-signature override; transitive/generic/interface diamonds, fake overrides, Java, and visibility are excluded."

[[requirements]]
id = "KS-INHERITANCE-0031"
statement = "A callable is inheritable only when its own and relevant accessor visibility is not private."
classification = "heuristic"
capabilities = ["definition", "completion", "references"]
status = "ignored"
tests = ["ks_inheritance_0031_private_callable_is_not_inherited"]
duplicates = ["ks_declarations_0434_private_member_is_accessible_only_in_its_declaration_scope"]
fixture = "DerivedSpec inherits BaseSpec, whose only hiddenSpec function is explicitly private."
heuristic_limitations = "Covers a direct Kotlin class edge and explicitly private function; property accessor visibility, Java, generics, indirect inheritance, and aliases are excluded."
ignore_reason = "Observed red: resolve_symbol returns BaseSpec.hiddenSpec as a member of DerivedSpec."
observed_failure = "resolve_symbol returns BaseSpec.hiddenSpec as a member of DerivedSpec."
expected_behavior = "DerivedSpec lookup for hiddenSpec must return no location."

[[requirements]]
id = "KS-INHERITANCE-0034"
statement = "An inheritable base callable is inherited when no inherited declaration subsumes it and no derived declaration overrides it."
classification = "heuristic"
capabilities = ["definition", "completion", "references"]
status = "active"
tests = ["ks_inheritance_0034_unopposed_inheritable_callable_is_inherited"]
duplicates = []
fixture = "DerivedSpec has one BaseSpec edge and no member competing with open inheritedSpec."
heuristic_limitations = "Covers one direct class edge and unique explicit function name; interfaces, overloads, generics, visibility accessors, Java, and transitive subsumption are excluded."

[[requirements]]
id = "KS-INHERITANCE-0035"
statement = "A concrete declaration inherited from a superclass suppresses matching abstract declarations from superinterfaces."
classification = "heuristic"
capabilities = ["definition", "completion", "implementation"]
status = "active"
tests = ["ks_inheritance_0035_superclass_concrete_callable_suppresses_interface_abstract_match"]
duplicates = []
fixture = "DerivedSpec combines concrete BaseSpec.renderSpec and abstract ContractSpec.renderSpec."
heuristic_limitations = "Covers one direct class plus one direct interface and a no-argument explicit String signature; overloads, generics, multiple interfaces, Java, and transitive graphs are excluded."

[[requirements]]
id = "KS-INHERITANCE-0036"
statement = "Inheriting several matching concrete declarations is a compile-time error unless the derived classifier overrides them."
classification = "exact"
capabilities = ["syntax diagnostics", "implementation", "code actions"]
status = "ignored"
tests = ["ks_inheritance_0036_multiple_inherited_concrete_matches_require_override"]
duplicates = []
fixture = "Two default interface renderSpec implementations compete; ValidSpec overrides while InvalidSpec does not."
ignore_reason = "Observed red: InvalidSpec has a clean CST and no inherited-concrete-conflict diagnostic."
observed_failure = "InvalidSpec has a clean CST and no inherited-concrete-conflict diagnostic."
expected_behavior = "InvalidSpec must receive a diagnostic requiring an explicit renderSpec override."

[[requirements]]
id = "KS-INHERITANCE-0037"
statement = "A concrete derived classifier inheriting an abstract callable must override it."
classification = "exact"
capabilities = ["syntax diagnostics", "implementation", "code actions"]
status = "ignored"
tests = ["ks_inheritance_0037_concrete_classifier_must_implement_inherited_abstract_callable"]
duplicates = ["ks_4_1_1_038_concrete_class_must_implement_inherited_abstract_members"]
fixture = "ValidSpec implements BaseSpec.renderSpec while concrete InvalidSpec omits it."
ignore_reason = "Observed red: concrete InvalidSpec has a clean CST and no missing-implementation diagnostic."
observed_failure = "concrete InvalidSpec has a clean CST and no missing-implementation diagnostic."
expected_behavior = "InvalidSpec must receive a diagnostic requiring renderSpec."

[[requirements]]
id = "KS-INHERITANCE-0038"
statement = "A derived classifier inheriting matching abstract and concrete declarations from superinterfaces must override them."
classification = "exact"
capabilities = ["syntax diagnostics", "implementation", "code actions"]
status = "ignored"
tests = ["ks_inheritance_0038_abstract_and_concrete_interface_matches_require_override"]
duplicates = []
fixture = "AbstractSpec and ConcreteSpec expose identical renderSpec; only ValidSpec overrides explicitly."
ignore_reason = "Observed red: InvalidSpec has a clean CST and no mixed abstract/concrete conflict diagnostic."
observed_failure = "InvalidSpec has a clean CST and no mixed abstract/concrete conflict diagnostic."
expected_behavior = "InvalidSpec must receive a diagnostic requiring an explicit renderSpec override."

[[requirements]]
id = "KS-INHERITANCE-0039"
statement = "An interface callable without a body is implicitly abstract and one with a body is implicitly open."
classification = "heuristic"
capabilities = ["implementation", "document symbols", "hover"]
status = "active"
tests = ["ks_inheritance_0039_interface_callables_are_implicitly_abstract_or_open"]
duplicates = []
fixture = "ImplementationSpec overrides bodyless abstractSpec and default-bodied defaultSpec from ContractSpec."
heuristic_limitations = "Covers direct no-argument Kotlin functions in one interface; properties, accessors, generics, overloads, Java defaults, and transitive inheritance are excluded."

[[requirements]]
id = "KS-INHERITANCE-0041"
statement = "A callable cannot be both private and open, abstract, or override."
classification = "exact"
capabilities = ["syntax diagnostics", "document symbols"]
status = "ignored"
tests = ["ks_inheritance_0041_private_callable_cannot_be_open_abstract_or_override"]
duplicates = []
fixture = "Plain private hiddenSpec competes with private open, abstract, and override functions."
ignore_reason = "Observed red: private open invalidSpec has a clean CST and no semantic diagnostic."
observed_failure = "private open invalidSpec has a clean CST and no semantic diagnostic."
expected_behavior = "Every private/open, private/abstract, and private/override combination must receive a diagnostic."

[[requirements]]
id = "KS-INHERITANCE-0042"
statement = "A derived callable overrides an overridable subsumed base callable when marked override."
classification = "heuristic"
capabilities = ["implementation", "definition"]
status = "active"
tests = ["ks_inheritance_0042_override_modifier_marks_subsuming_derived_callable"]
duplicates = ["ks_inheritance_0029_derived_matching_declaration_subsumes_base_declaration"]
fixture = "DerivedSpec directly overrides BaseSpec.renderSpec(Int) with the same explicit signature."
heuristic_limitations = "Covers one direct Kotlin class edge and simple explicit function signature; properties, accessors, generics, overloads, Java, and transitive subsumption are excluded."

[[requirements]]
id = "KS-INHERITANCE-0043"
statement = "An overriding function's return type must be a subtype of the base return type."
classification = "heuristic"
capabilities = ["syntax diagnostics", "hover", "implementation"]
status = "ignored"
tests = ["ks_inheritance_0043_overriding_function_return_type_must_be_subtype"]
duplicates = []
fixture = "String-over-Any validSpec competes with Any-over-String invalidSpec."
heuristic_limitations = "Covers explicit non-null Any and String return types on a direct class override; generics, aliases, nullable types, intersections, Java, and inferred returns are excluded."
ignore_reason = "Observed red: Any-over-String invalidSpec has a clean CST and no semantic diagnostic."
observed_failure = "Any-over-String invalidSpec has a clean CST and no semantic diagnostic."
expected_behavior = "invalidSpec.valueSpec return type must receive an incompatible-override diagnostic."

[[requirements]]
id = "KS-INHERITANCE-0044"
statement = "Base and overriding function suspendability must be identical."
classification = "exact"
capabilities = ["syntax diagnostics", "implementation", "hover"]
status = "ignored"
tests = ["ks_inheritance_0044_overriding_function_suspendability_must_match"]
duplicates = []
fixture = "ValidSpec preserves suspend; InvalidSpec removes it from loadSpec."
ignore_reason = "Observed red: non-suspend override of suspend loadSpec has a clean CST and no semantic diagnostic."
observed_failure = "non-suspend override of suspend loadSpec has a clean CST and no semantic diagnostic."
expected_behavior = "InvalidSpec.loadSpec must receive a suspendability mismatch diagnostic."

[[requirements]]
id = "KS-INHERITANCE-0045"
statement = "Override mutability cannot be stronger: a base val may become var, but a base var cannot become val."
classification = "exact"
capabilities = ["syntax diagnostics", "implementation", "hover"]
status = "ignored"
tests = ["ks_inheritance_0045_overriding_property_mutability_cannot_be_stronger"]
duplicates = []
fixture = "val-to-var ValidSpec competes with var-to-val InvalidSpec."
ignore_reason = "Observed red: var-to-val InvalidSpec has a clean CST and no semantic diagnostic."
observed_failure = "var-to-val InvalidSpec has a clean CST and no semantic diagnostic."
expected_behavior = "InvalidSpec.valueSpec must receive an override-mutability diagnostic."

[[requirements]]
id = "KS-INHERITANCE-0046"
statement = "An overriding non-mutable property's type must be a subtype of the base property type."
classification = "heuristic"
capabilities = ["syntax diagnostics", "hover", "implementation"]
status = "ignored"
tests = ["ks_inheritance_0046_read_only_override_property_type_may_be_covariant"]
duplicates = []
fixture = "String-over-Any val ValidSpec competes with Any-over-String val InvalidSpec."
heuristic_limitations = "Covers explicit non-null Any/String val types on a direct class edge; generics, aliases, nullability, accessors, Java, and inferred types are excluded."
ignore_reason = "Observed red: Any-over-String val has a clean CST and no semantic diagnostic."
observed_failure = "Any-over-String val has a clean CST and no semantic diagnostic."
expected_behavior = "InvalidSpec.valueSpec type must receive an incompatible-override diagnostic."

[[requirements]]
id = "KS-INHERITANCE-0047"
statement = "When both base and override properties are var, their types must be equivalent."
classification = "heuristic"
capabilities = ["syntax diagnostics", "hover", "implementation"]
status = "ignored"
tests = ["ks_inheritance_0047_mutable_override_property_type_must_be_equivalent"]
duplicates = []
fixture = "String/String var ValidSpec competes with String-over-Any var InvalidSpec."
heuristic_limitations = "Covers explicit non-null Any/String var types on a direct edge; aliases, generics, nullability, accessors, Java, and inferred types are excluded."
ignore_reason = "Observed red: String var overriding Any var has a clean CST and no semantic diagnostic."
observed_failure = "String var overriding Any var has a clean CST and no semantic diagnostic."
expected_behavior = "InvalidSpec.valueSpec must receive a mutable-property type-equivalence diagnostic."

[[requirements]]
id = "KS-INHERITANCE-0048"
statement = "A derived declaration cannot override a base declaration that is not overridable."
classification = "exact"
capabilities = ["syntax diagnostics", "implementation"]
status = "ignored"
tests = ["ks_inheritance_0048_non_overridable_base_callable_cannot_be_overridden"]
duplicates = []
fixture = "Open BaseSpec.renderSpec supports ValidSpec; final-by-default BaseSpec.renderSpec competes with InvalidSpec."
ignore_reason = "Observed red: override of final-by-default renderSpec has a clean CST and no semantic diagnostic."
observed_failure = "override of final-by-default renderSpec has a clean CST and no semantic diagnostic."
expected_behavior = "InvalidSpec.renderSpec must receive a non-overridable-base diagnostic."

[[requirements]]
id = "KS-INHERITANCE-0049"
statement = "A declaration subsuming an overridable base callable must use the override modifier."
classification = "exact"
capabilities = ["syntax diagnostics", "implementation", "code actions"]
status = "ignored"
tests = ["ks_inheritance_0049_overriding_callable_requires_override_modifier"]
duplicates = []
fixture = "Marked ValidSpec.renderSpec competes with unmarked same-signature InvalidSpec.renderSpec."
ignore_reason = "Observed red: unmarked InvalidSpec.renderSpec has a clean CST and no semantic diagnostic."
observed_failure = "unmarked InvalidSpec.renderSpec has a clean CST and no semantic diagnostic."
expected_behavior = "InvalidSpec.renderSpec must receive a missing-override diagnostic."

[[requirements]]
id = "KS-INHERITANCE-0051"
statement = "An explicitly specified override visibility cannot be stronger than the overridden declaration's visibility."
classification = "exact"
capabilities = ["syntax diagnostics", "implementation", "hover"]
status = "ignored"
tests = ["ks_inheritance_0051_explicit_override_visibility_cannot_be_stronger"]
duplicates = []
fixture = "public override of protected is valid; protected override of public is invalid."
ignore_reason = "Observed red: protected override of public renderSpec has a clean CST and no semantic diagnostic."
observed_failure = "protected override of public renderSpec has a clean CST and no semantic diagnostic."
expected_behavior = "InvalidSpec.renderSpec must receive an override-visibility diagnostic."

[[requirements]]
id = "KS-INHERITANCE-0054"
statement = "A same-named derived function that does not subsume the base declaration is an overload, not an override."
classification = "heuristic"
capabilities = ["document symbols", "implementation", "signature help"]
status = "active"
tests = ["ks_inheritance_0054_same_name_non_subsuming_function_is_overload_not_override"]
duplicates = []
fixture = "BaseSpec.renderSpec(Int) and DerivedSpec.renderSpec(String) remain two indexed unmarked functions."
heuristic_limitations = "Covers direct class inheritance and explicit Int/String single-parameter functions; generics, aliases, defaults, receivers, erasure, and conflict detection are excluded."
