id = "kmp-lsp"
name = "Kotlin LSP"
version = "0.2.0"
schema_version = 1
description = "Kotlin, Java, Swift and layout XML language support via kmp-lsp (tree-sitter, no JVM)."
repository = "https://github.com/Hessesian/kmp-lsp"
authors = ["kmp-lsp contributors"]

[lib]
kind = "Rust"
version = "0.5.0"

[language_servers.kmp-lsp]
name = "Kotlin LSP"
languages = ["Kotlin", "Java", "Swift", "XML"]
